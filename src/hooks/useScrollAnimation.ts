import React from 'react';

/**
 * Scroll animations have been removed across the website to ensure maximum performance and instant responsiveness.
 */
export function useScrollAnimation(containerRef?: React.RefObject<HTMLElement | null>, selector = '.gsap-reveal') {
  // No-op hook: Animations disabled for performance
}
